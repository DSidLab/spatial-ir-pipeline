nextflow_process {

    name "test preprocessing module"
    script "modules/local/preprocess/main.nf"
    process "PREPROCESS_SPATIAL_IR"

    test("should pass preprocessing"){
        tag "modules"
        tag "modules_local"
        tag "preprocess"
        when {
            process {
                """
                input[0] = [
                    [ id:'pt15' ], "$baseDir/tests/data/pt15", "$baseDir/tests/data/pt15/spatial_rna", "$baseDir/tests/data/pt15/adata.h5ad"
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                        process.out.output_paths,
                        process.out.versions,
                        path(process.out.versions[0]).yaml
                    ).match()
                }
            )
        }
    }
}
